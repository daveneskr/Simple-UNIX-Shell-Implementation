# Simple-UNIX-Shell-Implementation
A lightweight UNIX shell implemented in C as part of the COMP20200 UNIX Programming module at UCD. This project replicates core behaviour of a standard shell (like Bash), including command execution, process control, built-in commands, signal handling, and output redirection.
